---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: ospfinstances.protocols.eda.nokia.com
spec:
  group: protocols.eda.nokia.com
  names:
    kind: OSPFInstance
    listKind: OSPFInstanceList
    plural: ospfinstances
    singular: ospfinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.lastChange
      name: Last Change
      type: date
    - jsonPath: .status.operationalState
      name: Operational State
      type: string
    deprecated: true
    deprecationWarning: This version is deprecated and will no longer be supported
      in the next version.
    name: v1
    schema:
      openAPIV3Schema:
        description: OSPFInstance is the Schema for the ospfinstances API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OSPFInstanceSpec defines the desired state of OSPFInstance
            properties:
              addressFamily:
                description: Selects an address family for OSPFv3. It is mandatory
                  to specify at least one address family when OSPFv3 is selected.
                enum:
                - IPV4-UNICAST
                - IPV6-UNICAST
                type: string
              enabled:
                default: false
                description: Enables OSPF instance.
                type: boolean
              maxECMP:
                description: The maximum number of ECMP paths (next-hops).
                maximum: 256
                minimum: 1
                type: integer
              maxMetric:
                description: Configuration related to OSPF Max Metric / Overload.
                properties:
                  onBoot:
                    description: Set Max Metric on boot for the fixed period of time
                      (in seconds).
                    minimum: 10
                    type: integer
                  overload:
                    description: Enable Max Link Metric on all interfaces.
                    type: boolean
                type: object
              refBwGbps:
                description: Reference bandwidth (in Gbps) for automatic metric calculation.
                maximum: 3200
                minimum: 1
                type: integer
              timers:
                description: Configures OSPF timers.
                properties:
                  lsaTimers:
                    description: LSA Timers configuration.
                    properties:
                      accumulateMs:
                        description: Delay (in milliseconds) to gather LSAs before
                          advertising to neighbors
                        minimum: 0
                        type: integer
                      arrivalMs:
                        description: Minimum interval (in milliseconds) to accept
                          an identical LSA.
                        minimum: 0
                        type: integer
                      genHoldIntervalMs:
                        description: Hold interval (in milliseconds) for subsequent
                          LSA regeneration.
                        minimum: 0
                        type: integer
                      genInitialDelayMs:
                        description: Initial delay (in milliseconds) to generate the
                          first instance of LSAs.
                        minimum: 0
                        type: integer
                      genMaxDelayMs:
                        description: Maximum interval (in milliseconds) between two
                          consecutive regenerations (of the same LSA).
                        minimum: 0
                        type: integer
                    type: object
                  spfTimers:
                    description: SPF Timers configuration.
                    properties:
                      holdIntervalMs:
                        description: Hold interval for subsequent SPF calculations.
                        minimum: 1
                        type: integer
                      incrementalSPFDelayMs:
                        description: Delay (in milliseconds) before an incremental
                          SPF calculation starts.
                        minimum: 0
                        type: integer
                      initialDelayMs:
                        description: Initial SPF calculation delay (in milliseconds).
                        minimum: 0
                        type: integer
                      maxDelayMs:
                        description: Maximum interval (in milliseconds) between two
                          consecutive SPF calculations.
                        minimum: 1
                        type: integer
                    type: object
                type: object
              version:
                description: OSPF version to use. OSPFv2 is supported over IPv4-enabled
                  interfaces, OSPFv3 over IPv6-enabled interfaces.
                enum:
                - v2
                - v3
                type: string
            required:
            - enabled
            - version
            type: object
          status:
            description: OSPFInstanceStatus defines the observed state of OSPFInstance
            properties:
              health:
                description: Indicates the health score of the OSPF Instance.
                type: integer
              healthScoreReason:
                description: Indicates the reason for the health score.
                type: string
              lastChange:
                description: The time when the state of the resource last changed.
                type: string
              operationalState:
                description: Operational state of the OSPF Instance.
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .status.lastChangeTime
      name: Last Change
      type: date
    - jsonPath: .status.operationalState
      name: Operational State
      type: string
    name: v2
    schema:
      openAPIV3Schema:
        description: OSPFInstance is the Schema for the ospfinstances API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OSPFInstanceSpec defines the desired state of OSPFInstance
            properties:
              addressFamily:
                description: Selects an address family for OSPFv3. It is mandatory
                  to specify at least one address family when OSPFv3 is selected.
                enum:
                - IPv4Unicast
                - IPv6Unicast
                type: string
              enabled:
                default: false
                description: Enables OSPF instance.
                type: boolean
              maxECMP:
                description: The maximum number of ECMP paths (next-hops).
                maximum: 256
                minimum: 1
                type: integer
              maxMetric:
                description: Configuration related to OSPF Max Metric / Overload.
                properties:
                  onBoot:
                    description: Set Max Metric on boot for the fixed period of time
                      (in seconds).
                    minimum: 10
                    type: integer
                  overload:
                    description: Enable Max Link Metric on all interfaces.
                    type: boolean
                type: object
              referenceBandwidthGbps:
                description: Reference bandwidth (in Gbps) for automatic metric calculation.
                maximum: 3200
                minimum: 1
                type: integer
              timers:
                description: Configures OSPF timers.
                properties:
                  lsaTimers:
                    description: LSA Timers configuration.
                    properties:
                      accumulateMs:
                        description: Delay (in milliseconds) to gather LSAs before
                          advertising to neighbors
                        minimum: 0
                        type: integer
                      arrivalMs:
                        description: Minimum interval (in milliseconds) to accept
                          an identical LSA.
                        minimum: 0
                        type: integer
                      genHoldIntervalMs:
                        description: Hold interval (in milliseconds) for subsequent
                          LSA regeneration.
                        minimum: 0
                        type: integer
                      genInitialDelayMs:
                        description: Initial delay (in milliseconds) to generate the
                          first instance of LSAs.
                        minimum: 0
                        type: integer
                      genMaxDelayMs:
                        description: Maximum interval (in milliseconds) between two
                          consecutive regenerations (of the same LSA).
                        minimum: 0
                        type: integer
                    type: object
                  spfTimers:
                    description: SPF Timers configuration.
                    properties:
                      holdIntervalMs:
                        description: Hold interval for subsequent SPF calculations.
                        minimum: 1
                        type: integer
                      incrementalSPFDelayMs:
                        description: Delay (in milliseconds) before an incremental
                          SPF calculation starts.
                        minimum: 0
                        type: integer
                      initialDelayMs:
                        description: Initial SPF calculation delay (in milliseconds).
                        minimum: 0
                        type: integer
                      maxDelayMs:
                        description: Maximum interval (in milliseconds) between two
                          consecutive SPF calculations.
                        minimum: 1
                        type: integer
                    type: object
                type: object
              version:
                description: OSPF version to use. OSPFv2 is supported over IPv4-enabled
                  interfaces, OSPFv3 over IPv6-enabled interfaces.
                enum:
                - OSPFv2
                - OSPFv3
                type: string
            required:
            - enabled
            - version
            type: object
          status:
            description: OSPFInstanceStatus defines the observed state of OSPFInstance
            properties:
              health:
                description: Indicates the health score of the OSPF Instance.
                type: integer
              healthScoreReason:
                description: Indicates the reason for the health score.
                type: string
              lastChangeTime:
                description: The time when the state of the resource last changed.
                type: string
              operationalState:
                description: Operational state of the OSPF Instance.
                enum:
                - Up
                - Down
                - Degraded
                - Unknown
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
