---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: networksclientproviderinstances.services.eda.nokia.com
spec:
  group: services.eda.nokia.com
  names:
    kind: NetworksClientProviderInstance
    listKind: NetworksClientProviderInstanceList
    plural: networksclientproviderinstances
    singular: networksclientproviderinstance
  scope: Namespaced
  versions:
  - name: v2
    schema:
      openAPIV3Schema:
        description: |-
          NetworksClientProviderInstance is the Schema for the networksclientproviderinstances API.
          Instances are namespace-agnostic: they can be created in any namespace and target
          an EDA namespace via spec.namespace.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              Definition of one instance of the Network Client Provider.
              One Instance is always tied to a single Namespace to monitor.
            properties:
              confidenceLevelOffset:
                default: 0
                description: |-
                  Adjust the confidence level reported by this provider
                  for uncertain data, such as cross-referenced IP address information.
                  The base cross-table borrow confidence is 80%; this offset is added to it,
                  allowing operators to tune the reported confidence for their environment.
                  The resulting confidence is clamped to the range [0, 100].
                maximum: 80
                minimum: -80
                type: integer
              namespace:
                description: The Namespace this Instance will monitor and collect
                  Client information for.
                type: string
            required:
            - namespace
            type: object
          status:
            description: Status of this Networks Client Provider instance.
            properties:
              errorMessage:
                description: |-
                  ErrorMessage contains a human-readable description of the last error when
                  OperationalStatus is Error. Empty when the provider is Pending or Operational.
                type: string
              operationalStatus:
                description: Reports whether the provider is running and discovering
                  clients.
                enum:
                - Pending
                - Operational
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
