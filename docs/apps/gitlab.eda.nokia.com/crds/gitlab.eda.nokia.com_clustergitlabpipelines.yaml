---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: clustergitlabpipelines.gitlab.eda.nokia.com
spec:
  group: gitlab.eda.nokia.com
  names:
    kind: ClusterGitlabPipeline
    listKind: ClusterGitlabPipelineList
    plural: clustergitlabpipelines
    singular: clustergitlabpipeline
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterGitlabPipeline is the Schema for the clustergitlabpipelines
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterGitlabPipeline
            properties:
              instance:
                description: Gitlab instance name.
                type: string
              parameters:
                description: Parameters to pass to the workflow.
                items:
                  properties:
                    name:
                      description: Parameter name
                      type: string
                    value:
                      description: Parameter value
                      properties:
                        dynamicValue:
                          description: Dynamic value.
                          properties:
                            field:
                              description: Field to watch.
                              type: string
                            path:
                              description: Path to the value.
                              type: string
                            where:
                              description: Where clause to filter the results.
                              type: string
                          type: object
                        staticValue:
                          description: Static value.
                          type: string
                      type: object
                  type: object
                type: array
              pipeline:
                description: Pipeline to run.
                type: string
              ref:
                description: Reference is the branch, tag or commit hash to run the
                  workflow.
                type: string
              repo:
                description: Repository to run the workflow.
                type: string
              trigger:
                description: Trigger conditions to run the pipeline.
                properties:
                  alarm:
                    description: Alarm to Create the issue.
                    properties:
                      exclude:
                        description: Excluded alarm types that don't trigger the workflow
                        items:
                          type: string
                        type: array
                      include:
                        description: Included alarm types that trigger the workflow
                        items:
                          type: string
                        type: array
                      namespaces:
                        description: Namespaces to watch for alarms
                        items:
                          type: string
                        type: array
                    type: object
                  query:
                    description: Query to trigger the workflow
                    properties:
                      fields:
                        description: List of fields to watch
                        items:
                          type: string
                        type: array
                      path:
                        description: Path to the EDB table to watch
                        type: string
                      where:
                        description: Where clause to filter the results
                        type: string
                    type: object
                type: object
            type: object
          status:
            description: status defines the observed state of ClusterGitlabPipeline
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the ClusterGitlabPipeline resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
