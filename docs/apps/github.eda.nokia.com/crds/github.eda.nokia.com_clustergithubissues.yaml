---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: clustergithubissues.github.eda.nokia.com
spec:
  group: github.eda.nokia.com
  names:
    kind: ClusterGitHubIssue
    listKind: ClusterGitHubIssueList
    plural: clustergithubissues
    singular: clustergithubissue
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterGitHubIssue is the Schema for the clustergithubissues
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterGitHubIssue
            properties:
              closeOnResolve:
                description: Close the issue on resolve.
                type: boolean
              instance:
                description: Github instance name.
                type: string
              issue:
                description: Issue to create.
                properties:
                  assignees:
                    description: Assignees of the issue.
                    items:
                      type: string
                    type: array
                  body:
                    description: Body of the issue.
                    type: string
                  labels:
                    description: Labels of the issue.
                    items:
                      type: string
                    type: array
                  milestone:
                    description: Milestone of the issue.
                    type: string
                  title:
                    description: Title of the issue.
                    type: string
                type: object
              repo:
                description: Repository to create the issue.
                type: string
              trigger:
                description: Trigger conditions to create the issue.
                properties:
                  alarm:
                    description: Alarm to trigger the workflow
                    properties:
                      exclude:
                        description: Excluded alarm types that don't trigger the workflow
                        items:
                          type: string
                        type: array
                      include:
                        description: Included alarm types that trigger the workflow
                        items:
                          type: string
                        type: array
                      namespaces:
                        description: Namespaces to watch for alarms
                        items:
                          type: string
                        type: array
                    type: object
                  query:
                    description: Query to trigger the workflow
                    properties:
                      fields:
                        description: List of fields to watch
                        items:
                          type: string
                        type: array
                      path:
                        description: Path to the EDB table to watch
                        type: string
                      where:
                        description: Where clause to filter the results
                        type: string
                    type: object
                type: object
            type: object
          status:
            description: status defines the observed state of ClusterGitHubIssue
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
