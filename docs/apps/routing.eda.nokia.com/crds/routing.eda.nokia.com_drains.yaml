---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: drains.routing.eda.nokia.com
spec:
  group: routing.eda.nokia.com
  names:
    kind: Drain
    listKind: DrainList
    plural: drains
    singular: drain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.enabled
      name: Enabled
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Drain is the Schema for the drains API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Drain allows for the controlled disabling or draining of
              specific DefaultRouters, either by selecting them through labels or
              directly referencing them, ensuring traffic is safely rerouted or dropped
              before the routers are decommissioned.
            properties:
              defaultRouterSelectors:
                description: Selector to use when selecting DefaultRouters to drain.
                items:
                  type: string
                type: array
              defaultRouters:
                description: Reference to DefaultRouters to drain.
                items:
                  type: string
                type: array
              enabled:
                default: true
                description: Enable this Drain.
                type: boolean
            type: object
          status:
            description: DrainStatus defines the observed state of Drain.
            properties:
              defaultRouters:
                description: List of DefaultRouters this Drain has been applied to
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .spec.enabled
      name: Enabled
      type: string
    deprecated: true
    deprecationWarning: This version is deprecated and will no longer be supported
      in the next version.
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Drain is the Schema for the drains API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Drain allows for the controlled disabling or draining of
              specific DefaultRouters, either by selecting them through labels or
              directly referencing them, ensuring traffic is safely rerouted or dropped
              before the routers are decommissioned.
            properties:
              defaultRouterSelector:
                description: Selector to use when selecting DefaultRouters to drain.
                items:
                  type: string
                type: array
              defaultRouters:
                description: Reference to DefaultRouters to drain.
                items:
                  type: string
                type: array
              enabled:
                default: true
                description: Enable this Drain.
                type: boolean
            type: object
          status:
            description: DrainStatus defines the observed state of Drain.
            properties:
              defaultRouters:
                description: List of DefaultRouters this Drain has been applied to
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
