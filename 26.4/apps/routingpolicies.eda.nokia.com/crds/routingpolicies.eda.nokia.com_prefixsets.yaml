---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: prefixsets.routingpolicies.eda.nokia.com
spec:
  group: routingpolicies.eda.nokia.com
  names:
    kind: PrefixSet
    listKind: PrefixSetList
    plural: prefixsets
    singular: prefixset
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PrefixSet is the Schema for the prefixsets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PrefixSet defines a collection of IP prefixes, which may
              include specific CIDR blocks or a range of prefixes. This set is typically
              used for matching routes or implementing routing policies.
            properties:
              configuredName:
                description: The name of the prefixset to configure on the device.
                type: string
              prefixes:
                description: List of IPv4 or IPv6 prefixes in CIDR notation.
                items:
                  properties:
                    endRange:
                      description: The end range when using a range to match prefixes.
                      maximum: 128
                      minimum: 0
                      type: integer
                    exact:
                      description: Indicates if it is an exact match. Ignores the
                        StartRange and EndRange if this param is set.
                      type: boolean
                    prefix:
                      description: The IPv4 or IPv6 prefix in CIDR notation with mask.
                      type: string
                    startRange:
                      description: If specifying a range, this is the start of the
                        range.
                      maximum: 128
                      minimum: 0
                      type: integer
                  required:
                  - prefix
                  type: object
                type: array
            required:
            - prefixes
            type: object
          status:
            description: PrefixSetStatus defines the observed state of PrefixSet.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - deprecated: true
    deprecationWarning: This version is deprecated and will no longer be supported
      in the next version.
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PrefixSet is the Schema for the prefixsets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PrefixSet defines a collection of IP prefixes, which may
              include specific CIDR blocks or a range of prefixes. This set is typically
              used for matching routes or implementing routing policies.
            properties:
              configuredName:
                description: The name of the prefixset to configure on the device.
                type: string
              prefix:
                description: List of IPv4 or IPv6 prefixes in CIDR notation.
                items:
                  properties:
                    endRange:
                      description: The end range when using a range to match prefixes.
                      maximum: 128
                      minimum: 0
                      type: integer
                    exact:
                      description: Indicates if it is an exact match. Ignores the
                        StartRange and EndRange if this param is set.
                      type: boolean
                    prefix:
                      description: The IPv4 or IPv6 prefix in CIDR notation with mask.
                      type: string
                    startRange:
                      description: If specifying a range, this is the start of the
                        range.
                      maximum: 128
                      minimum: 0
                      type: integer
                  required:
                  - prefix
                  type: object
                type: array
            required:
            - prefix
            type: object
          status:
            description: PrefixSetStatus defines the observed state of PrefixSet.
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
