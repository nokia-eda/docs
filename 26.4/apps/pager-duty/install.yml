apiVersion: appstore.eda.nokia.com/v1
kind: AppInstaller
metadata:
  name: pager-duty-install
  namespace: eda-system
spec:
  operation: install
  apps:
    - appId: pagers.eda.nokia.com
      catalog: eda-catalog-builtin-apps
      version:
        type: alias
        value: latest
