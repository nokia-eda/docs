---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: satellitestates.bootstrap.eda.nokia.com
spec:
  group: bootstrap.eda.nokia.com
  names:
    kind: SatelliteState
    listKind: SatelliteStateList
    plural: satellitestates
    singular: satellitestate
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: SatelliteState is the Schema for the satellitestates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SatelliteStateSpec defines the desired state of SatelliteState
            properties:
              hostUplinks:
                items:
                  properties:
                    hostPort:
                      description: HostPort interface of the satellite uplink.
                      type: string
                    satellitePort:
                      description: Satellite interface of the satellite uplink.
                      type: string
                  required:
                  - hostPort
                  - satellitePort
                  type: object
                type: array
              hostname:
                type: string
              id:
                type: string
            required:
            - hostname
            - id
            type: object
          status:
            description: SatelliteStateStatus defines the observed state of SatelliteState
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
