---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: platformbackups.coreext.eda.nokia.com
spec:
  group: coreext.eda.nokia.com
  names:
    kind: PlatformBackup
    listKind: PlatformBackupList
    plural: platformbackups
    singular: platformbackup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PlatformBackup is the Schema for the platformbackups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PlatformBackupSpec defines the desired state of PlatformBackup
            properties:
              excludeIdentityGitRepo:
                default: false
                description: Exclude the identity Git repository from the backup
                type: boolean
              excludeSecurityGitRepo:
                default: false
                description: Exclude the security Git repository from the backup
                type: boolean
              name:
                default: ""
                description: Name of the tarball. [default=eda-backup-<cluster-member-name>-<date-and-time>.tar.gz]
                type: string
              timeout:
                default: 5m
                description: Timeout duration for backup. [default=5m]
                type: string
            type: object
          status:
            description: PlatformBackupStatus defines the observed state of PlatformBackup
            properties:
              result:
                description: Result
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
