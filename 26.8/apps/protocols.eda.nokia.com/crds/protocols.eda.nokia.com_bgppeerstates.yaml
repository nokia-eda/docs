---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: bgppeerstates.protocols.eda.nokia.com
spec:
  group: protocols.eda.nokia.com
  names:
    kind: BGPPeerState
    listKind: BGPPeerStateList
    plural: bgppeerstates
    singular: bgppeerstate
  scope: Namespaced
  versions:
  - deprecated: true
    deprecationWarning: This version is deprecated and will no longer be supported
      in the next version.
    name: v1
    schema:
      openAPIV3Schema:
        description: BGPPeerState is the Schema for the bgppeerstates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BGPPeerStateSpec defines the desired state of BGPPeerState
            properties:
              afiSAFI:
                description: List of configured AFI-SAFI on the BGP peer
                items:
                  type: string
                type: array
              defaultNetworkInstance:
                description: Denotes if the router is a DefaultRouter or Router
                type: boolean
              dynamicNeighbor:
                default: false
                description: When set to true the PeerDefaultInterface is added to
                  the dynamic-neighbor list for dynamic peering.
                type: boolean
              group:
                description: Reference to a BGPGroup
                type: string
              interface:
                description: Reference to the interface whose IP is used as a source
                  IP for the BGP session.
                type: string
              interfaceKind:
                description: Reference to a the Kind of interface whose IP is used
                  as a source IP for the BGP session.
                type: string
              networkInstanceName:
                description: The name of the network-instance or VPRN in which the
                  BGP peer is configured
                type: string
              node:
                description: The Node on which the BGP peer configuration resides
                type: string
              nodeInterface:
                description: Node interface of the default interface which is configured
                  to peer as a dynamic neighbor
                type: string
              operatingSystem:
                description: Operating System of the Node
                type: string
              peerIP:
                description: The IP of the BGP peer
                type: string
              router:
                description: Router to which the BGP peer is attached
                type: string
              subInterfaceIndex:
                description: Sub interface index of the default interface which is
                  configured to peer as a dynamic neighbor
                type: integer
            required:
            - defaultNetworkInstance
            - dynamicNeighbor
            - networkInstanceName
            - router
            type: object
          status:
            description: BGPPeerStateStatus defines the observed state of BGPPeerState
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - name: v2
    schema:
      openAPIV3Schema:
        description: BGPPeerState is the Schema for the bgppeerstates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BGPPeerStateSpec defines the desired state of BGPPeerState
            properties:
              afiSAFIs:
                description: List of configured AFI-SAFI on the BGP peer
                items:
                  type: string
                type: array
              defaultNetworkInstance:
                description: Denotes if the router is a DefaultRouter or Router
                type: boolean
              dynamicNeighbor:
                default: false
                description: When set to true the PeerDefaultInterface is added to
                  the dynamic-neighbor list for dynamic peering.
                type: boolean
              group:
                description: Reference to a BGPGroup
                type: string
              interface:
                description: Reference to the interface whose IP is used as a source
                  IP for the BGP session.
                type: string
              interfaceKind:
                description: Reference to a the Kind of interface whose IP is used
                  as a source IP for the BGP session.
                type: string
              networkInstanceName:
                description: The name of the network-instance or VPRN in which the
                  BGP peer is configured
                type: string
              node:
                description: The Node on which the BGP peer configuration resides
                type: string
              nodeInterface:
                description: Node interface of the default interface which is configured
                  to peer as a dynamic neighbor
                type: string
              operatingSystem:
                description: Operating System of the Node
                type: string
              peerIP:
                description: The IP of the BGP peer
                type: string
              router:
                description: Router to which the BGP peer is attached
                type: string
              subInterfaceIndex:
                description: Sub interface index of the default interface which is
                  configured to peer as a dynamic neighbor
                type: integer
            required:
            - defaultNetworkInstance
            - dynamicNeighbor
            - networkInstanceName
            - router
            type: object
          status:
            description: BGPPeerStateStatus defines the observed state of BGPPeerState
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
