---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: queues.qos.eda.nokia.com
spec:
  group: qos.eda.nokia.com
  names:
    kind: Queue
    listKind: QueueList
    plural: queues
    singular: queue
  scope: Namespaced
  versions:
  - deprecated: true
    deprecationWarning: This version is deprecated and will no longer be supported
      in the next version.
    name: v1
    schema:
      openAPIV3Schema:
        description: Queue is the Schema for the queues API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: The Queue resource is used to define the properties of a
              queue, which can then be referenced by other resources.
            properties:
              queueID:
                description: The ID of the queue on which to apply the properties.  This
                  is mandatory for usage of queus on SROS and is ignored on other
                  operating systems.
                type: integer
              queueType:
                description: QueueType specifies whether this is a normal queue or
                  a PFC queue
                enum:
                - Normal
                - Pfc
                type: string
              trafficType:
                description: The traffic type of the queue, either unicast or multicast.
                enum:
                - Unicast
                - Multicast
                type: string
            required:
            - queueType
            - trafficType
            type: object
          status:
            description: QueueStatus defines the observed state of Queue
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - name: v2
    schema:
      openAPIV3Schema:
        description: Queue is the Schema for the queues API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: The Queue resource is used to define the properties of a
              queue, which can then be referenced by other resources.
            properties:
              queueID:
                description: The ID of the queue on which to apply the properties.  This
                  is mandatory for usage of queus on SROS and is ignored on other
                  operating systems.
                type: integer
              queueType:
                description: QueueType specifies whether this is a normal queue or
                  a PFC queue
                enum:
                - Normal
                - PFC
                type: string
              trafficType:
                description: The traffic type of the queue, either unicast or multicast.
                enum:
                - Unicast
                - Multicast
                type: string
            required:
            - queueType
            - trafficType
            type: object
          status:
            description: QueueStatus defines the observed state of Queue
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
