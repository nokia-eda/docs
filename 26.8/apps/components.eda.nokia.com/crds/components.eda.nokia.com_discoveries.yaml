---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: discoveries.components.eda.nokia.com
spec:
  group: components.eda.nokia.com
  names:
    kind: Discovery
    listKind: DiscoveryList
    plural: discoveries
    singular: discovery
  scope: Namespaced
  versions:
  - deprecated: true
    deprecationWarning: This version is deprecated and will no longer be supported
      in the next version.
    name: v1
    schema:
      openAPIV3Schema:
        description: Discovery is the Schema for the discoveries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DiscoverySpec defines the desired state of Discovery
            properties:
              nodeSelector:
                description: Selector to use when selecting TopoNodes to discover
                  components for.
                items:
                  type: string
                type: array
              nodes:
                description: ' TopoNodes to discover components for.'
                items:
                  type: string
                type: array
              operatingSystem:
                description: Operating system to match against when selecting TopoNodes
                enum:
                - srl
                - sros
                - eos
                - sonic
                - ios-xr
                - nxos
                type: string
            type: object
          status:
            description: DiscoveryStatus defines the observed state of Discovery
            properties:
              nodes:
                description: List of TopoNodes component discovery is running for
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - name: v2
    schema:
      openAPIV3Schema:
        description: Discovery is the Schema for the discoveries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DiscoverySpec defines the desired state of Discovery
            properties:
              nodeSelectors:
                description: Selector to use when selecting TopoNodes to discover
                  components for.
                items:
                  type: string
                type: array
              nodes:
                description: ' TopoNodes to discover components for.'
                items:
                  type: string
                type: array
              operatingSystem:
                description: Operating system to match against when selecting TopoNodes
                enum:
                - srl
                - sros
                - eos
                - sonic
                - ios-xr
                - nxos
                type: string
            type: object
          status:
            description: DiscoveryStatus defines the observed state of Discovery
            properties:
              nodes:
                description: List of TopoNodes component discovery is running for
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
