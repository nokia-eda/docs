---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: clustergithubinstances.github.eda.nokia.com
spec:
  group: github.eda.nokia.com
  names:
    kind: ClusterGitHubInstance
    listKind: ClusterGitHubInstanceList
    plural: clustergithubinstances
    singular: clustergithubinstance
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterGitHubInstance is the Schema for the clustergithubinstances
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterGitHubInstance
            properties:
              apiBaseURL:
                description: Base URL for the Github API. Leave empty for the public
                  Github API base URL.
                type: string
              authSecretRef:
                description: Reference to the secret containing the Github token.
                properties:
                  key:
                    description: Credentials key in the secret.
                    type: string
                  name:
                    description: Secret Name containing the credentials.
                    type: string
                type: object
            required:
            - authSecretRef
            type: object
          status:
            description: status defines the observed state of ClusterGitHubInstance
            properties:
              connected:
                description: Whether the instance is connected.
                type: boolean
              error:
                description: Error message if the instance is not connected.
                type: string
              lastChecked:
                description: Last checked time.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
